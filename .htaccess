############################################
# 1. Motor de reescritura
############################################
RewriteEngine On

# --- 1.1 Forzar HTTPS y canónico sin‑www -------------
# 👉 MAMP: comenta este bloque completo (no hay SSL local)
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^ https://polizaderentas.com%{REQUEST_URI} [L,R=301]

# --- 1.2 URLs amigables ------------------------------
#     /investigacion-inquilinos  →  investigacion_inquilinos.php
RewriteRule ^investigacion-inquilinos/?$ investigacion_inquilinos.php [L,QSA]
RewriteRule ^poliza-juridica/?$        poliza_juridica.php        [L,QSA]
#  (añade reglas equivalentes para más páginas)

############################################
# 2. Cabeceras de rendimiento
############################################
# --- 2.1 Compresión Brotli / Gzip ---
# 👉 MAMP: si tu Apache no tiene mod_brotli/mod_deflate, comenta estas secciones
<IfModule mod_brotli.c>
  BrotliCompressionQuality 5
  AddOutputFilterByType BROTLI_COMPRESS text/html text/css application/javascript image/svg+xml
</IfModule>

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript image/svg+xml
</IfModule>

# --- 2.2 Caché navegador (estáticos) ---
# 👉 MAMP: mod_expires suele estar activo; si no, comenta el bloque
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/webp  "access plus 1 year"
  ExpiresByType image/jpeg  "access plus 1 year"
  ExpiresByType image/png   "access plus 1 year"
  ExpiresByType font/woff2  "access plus 1 year"
  ExpiresByType text/css    "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# --- 2.3 Cache‑Control ---
<IfModule mod_headers.c>
  <FilesMatch "\.(css|js)$">
    Header set Cache-Control "public, max-age=2592000, immutable"
  </FilesMatch>
  <FilesMatch "\.(jpg|jpeg|png|webp|svg|gif|woff2)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
</IfModule>

############################################
# 3. Cabeceras de seguridad
############################################
# 👉 MAMP: sin HTTPS no sirve HSTS; comenta si lo desactivaste arriba
<IfModule mod_headers.c>
  Header always set Strict-Transport-Security "max-age=63072000; includeSubDomains; preload"
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set X-XSS-Protection "1; mode=block"
</IfModule>

############################################
# 4. Manejo de errores y acceso
############################################
ErrorDocument 404 /404.php

# Redirigir a 404 si el recurso no existe
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ ./404.php [L]

# Prevenir listados de directorios
Options -Indexes
